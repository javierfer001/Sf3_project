{% extends 'BackEndBundle::index.html.twig' %}

{% block table -%}
    <h1><a class="btn btn-danger" href="{{ path('category') }}">Cetegory List</a> SubCategory list</h1>
    <a class="btn btn-success" href="{{ path('subcategory_new',{ 'id': 0,'name': 'null','idCategory':idCategory}) }}">
        Create SubCategory
    </a>
    <br>
    <br>
    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
        <thead>
            <tr>
                <th>Sub Category</th>
                <th>Sub Category(es)</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
                <tr>
                    <td>{{ entity.subCategory }}</td>
                    <td>{{ entity.subCategoryEs }}</td>
                    <td>
                        <ul>
                            <a class="btn btn-success" href="{{ path('subcategory_new', { 'id': entity.id,'name': entity.subCategory|url_encode,'idCategory':idCategory}) }}">edit</a>
                            <a class="btn btn-danger" href="{{ path('subcategory_delete', { 'id': entity.id, 'idCategory':idCategory}) }}">Delete</a>
                        </ul>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>

        </li>
    </ul>
{% endblock %}
